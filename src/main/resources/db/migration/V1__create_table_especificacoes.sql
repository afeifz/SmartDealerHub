CREATE TABLE especificacoes_ford (
                                     id BIGINT AUTO_INCREMENT PRIMARY KEY,
                                     categoria VARCHAR(100),
                                     equipamento VARCHAR(255),
                                     versao_xlt VARCHAR(50),
                                     versao_limited VARCHAR(50),
                                     versao_limited_plus VARCHAR(50)
);